<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <!-- Microsoft's official KQL parser, the same one used by Azure Data Explorer and Sentinel tooling -->
    <PackageReference Include="Microsoft.Azure.Kusto.Language" Version="*" />
  </ItemGroup>
</Project>
